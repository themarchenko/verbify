# Commit Skill

1. Run `pnpm build` and fix any errors — never commit with build errors
2. Check for duplicate padding/spacing in changed UI files
3. Verify translation keys match existing casing conventions in messages/\*.json
4. Stage changes and create a conventional commit message
5. Never commit with build errors
